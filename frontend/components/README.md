# Components Directory

Place reusable UI and feature-specific React components here.
- `ui/`: Generic styling components (Buttons, Modals, Inputs, GlassCard)
- `dashboard/`: Components specific to the dashboard pages
- `proctor/`: Video camera monitoring feeds, tracking canvas overlays
- `exam/`: Exam viewport, quiz engines, timers
