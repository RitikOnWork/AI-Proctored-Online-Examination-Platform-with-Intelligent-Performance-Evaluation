# Core Library Directory

Configure external libraries, utilities, clients, and wrappers here:
- `api-client.ts`: Custom fetch or axios wrapper pre-configured with authorization header tokens and interceptors.
- `utils.ts`: Tailwind class mergers (`clsx`, `tailwind-merge` utility) or date helpers.
